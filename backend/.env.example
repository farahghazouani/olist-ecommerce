# Ancienne variable PostgreSQL (supprimee) :
# DATABASE_URL=postgresql://user:password@localhost:5432/olist_bi

# MongoDB local :
MONGODB_URI=mongodb://localhost:27017
# MongoDB Atlas (version en ligne) :
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net

MONGODB_DB_NAME=olist_bi
